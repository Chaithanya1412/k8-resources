appVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    project: roboshop
    component: frontend
    environment: dev
  ports:
    - protocal: TCP
      port: 80 # service port
      target port: 80 # container port

